{
    "name": "manta-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd server && npm install --include=dev && npm run build",
        "start": "cd server && npm start"
    },
    "engines": {
        "node": "20.x",
        "npm": "10.x"
    }
}